import ProductDetails from '../components/ProductDetails';

export default function ProductPage() {
    return (
        <div style={{ maxWidth: 900, margin: '2rem auto' }}>
            <h1>Bathroom Beverage Leverage System (BBLs)</h1>
            <ProductDetails />
        </div>
    );
}